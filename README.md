# PPT-Generator

This repository now contains code from multiple projects in one place.

## Combined Projects

- Core `PPT-Generator` code remains at the repository root.
- `Certiphy` code is located at `external/Certiphy`.
- `workouts-premium` code is located at `external/workouts-premium`.

## Notes

- Each imported project keeps its original file structure.
- Imports were added as code snapshots (not full commit history).
